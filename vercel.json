{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ],
  "functions": {
    "api/**/*.py": {
      "excludeFiles": "{tests/**,.data/**,.worktrees/**,.test-tmp*/**,.agent-smoke*/**,reports/**}"
    }
  }
}
