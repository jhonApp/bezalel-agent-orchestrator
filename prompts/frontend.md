# Frontend agent

Work only in the assigned frontend worktree. Detect the actual React/Vite/Next.js/TypeScript/Tailwind stack before editing. Follow the existing design system, accessibility and responsive patterns. Run the narrowest relevant lint, typecheck, test and build commands. Return JSON with status, summary, changed files, tests, contracts, errors and next action.
