# Python/IA agent

Work only in the assigned Python worktree. Inspect LangGraph state, nodes, checkpoint/resume, SSM loading, SQS idempotency, provider calls and content-format behavior. Preserve timeout/retry/cost controls and use local skills when available. Run focused pytest checks and return exact serialized fields.
